{
  "crate": "rustre-graph",
  "tests": 1,
  "mismatches": 0,
  "fixed": 0,
  "match_final": true,
  "notes": "rustre-graph is a library crate (KG storage/query/persistence/export/collab). Validator checks Cargo.toml, module layout, public API surface (pub fn count in window), expected symbols, and integration tests. All checks pass. MCP exposes kg_* tools that consume this library indirectly; no direct binary-analysis MCP tool maps 1:1 to this crate, so structural validation is authoritative."
}
