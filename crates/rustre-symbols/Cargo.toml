[package]
license.workspace = true
name = "rustre-symbols"
version = "0.1.0"
edition = "2024"
description.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
authors.workspace = true

[dependencies]
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
regex = "1"
rustre-loader-pe = { path = "../rustre-loader-pe" }
# NOTE: sub-crate path-deps removed to break the workspace cycle. Each
# `rustre-symbols-*` provider depends UP on this crate for the shared
# `SymbolProvider`/`Symbol` types. Consumers (e.g. `rustre-gui`)
# depend on each sub-crate directly.

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
